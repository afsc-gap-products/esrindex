library(rema)
library(ggplot2)
library(scales)

# vignette(topic = "ex2_cpue")
# vignette(topic = "ex3_zeros")
# vignette(topic = "rema_equations") # view technical details offline

plot_rema_region(x = AI_INDICATOR)
plot_rema_region(x = GOA_INDICATOR)
plot_rema_region(x = EBS_INDICATOR)
plot_rema_region(x = NBS_INDICATOR)
